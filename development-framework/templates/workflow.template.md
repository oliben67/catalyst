# `WORKFLOW-NNNNNN` — short title

> A process-definition document, not a unit of work: no parent
> epic/story, never itself "done" (`Rules-of-Rules.md` §8,
> `rules-of-work-items.md` §7). Documents how a category of work moves
> through its steps.

| Field | Value |
|---|---|
| **ID** | `WORKFLOW-NNNNNN` |
| **Status** | `Active` / `Deprecated` |
| **Defined** | YYYY-MM-DD |
| **Signed-off-by** | name of the registered user (`IAM/users/users.json`) who signed this workflow — see `CODE-OF-CONDUCT.md` §2 |

## Purpose

What this process governs, and why it exists as a documented procedure
rather than tribal knowledge.

## Steps

1. …
2. …

## Gates / exit criteria

What must be true for work to move from one step to the next, or to be
considered complete.

## Related

Other `WORKFLOW-`/`EPIC-`/rule domain IDs this depends on, supersedes, or
overlaps with. If this workflow replaces an older one, name it and mark
that one `Deprecated` here (never delete — retire in place).
