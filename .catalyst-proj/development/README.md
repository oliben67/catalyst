# Development

Dev artifacts (`bugs/`, `house-keeping/`, `meta-tags/` — none yet, each
with its own index), the machine-only `BACKLOG.md` (`/show-backlog`
regenerates it, never hand-edited), named roadmaps under `roadmaps/`
(`/roadmap-add` etc.), and the user/role registry (`users.json`,
`roles.json` — `/user-*`/`/role-*` commands only). See
`CODE-OF-CONDUCT.md` §2/§3.
