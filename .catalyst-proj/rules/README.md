# Rules

Meta-rules in `Rules-of-Rules.md`; the one rule document (`framework/fw-framework-rules.md`,
prefix `fw`) lives in its type directory `framework/`. `TEMPLATE-RULE.md`
is the only template file allowed at this root. `rules.md` is the global
index — every rule ID must appear there (`Rules-of-Rules.md` §5).
