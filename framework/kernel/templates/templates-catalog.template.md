# `{{TYPE}}` templates — catalog

> Copy this file to `{{TYPE_DIR}}/templates/templates-{{type}}.md` on
> first deploy, seeded with one row for `v1` (`INVARIANTS.md` INV-20,
> `Rules-of-Rules.md` §15). Append a new row — never edit an existing
> one — each time `TEMPLATE-{{TYPE}}-vN.md`'s content changes
> meaningfully enough to warrant a new version. This file *is* where the
> hard rule's "timestamped" half is satisfied: the template file itself
> carries no date, this table does.

| Version | File | Timestamp | Notes |
|---|---|---|---|
| v1 | `TEMPLATE-{{TYPE}}-v1.md` | {{YYYY-MM-DD}} | Initial version. |
