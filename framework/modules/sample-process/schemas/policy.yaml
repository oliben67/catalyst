id_prefix: POLICY
name: Policy
plural_name: Policies
folder: policies
grounding: none

fields:
  - name: ID
    kind: text
    required: true
  - name: Status
    kind: enum
    required: true
    allowed_values:
      - Draft
      - Active
      - Retired

workflow:
  initial: Draft
  states:
    - Draft
    - Active
    - Retired
  closed_states:
    - Retired
